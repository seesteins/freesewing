const in_to_mm = 25.4

function draftCinchTop({ Path, Point, paths, points, measurements, options, store, part }) {
  return part
}

function draftCinchChannel({ Path, Point, paths, points, measurements, options, store, part }) {
  return part
}
